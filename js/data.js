/* This file is no longer used and is not loaded by index.html.
   It has been replaced by:
     js/config.js   — party details, the script, battle tuning (edit this)
     js/dex.js      — loads data/*.json and builds battle-ready Pokémon
     data/*.json    — the 267 Pokémon, their moves, and the type chart
   Safe to delete. */
